<Window x:Class="SLAM.Reborn.WarningWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:SLAM.Reborn"
  mc:Ignorable="d" Title="Warning" Height="250" Width="450" WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize" AllowsTransparency="False" Background="#24282F" Foreground="White"
  FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif" ShowInTaskbar="False">
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="32" GlassFrameThickness="0" CornerRadius="0" />
  </WindowChrome.WindowChrome>
  <Window.Resources>
    <Style x:Key="TitleBarButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="#DDDDDD" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Width" Value="46" />
      <Setter Property="Height" Value="32" />
      <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#3F3F41" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="CloseButtonStyle" TargetType="Button"
      BasedOn="{StaticResource TitleBarButtonStyle}">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#E81123" />
          <Setter Property="Foreground" Value="White" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="ActionButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="#20232a" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="15,8" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#3F3F46" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PrimaryActionButtonStyle" TargetType="Button"
      BasedOn="{StaticResource ActionButtonStyle}">
      <Setter Property="Background" Value="#1A9fff" />
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#1C97EA" />
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="32" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0" Background="#252526">
      <TextBlock x:Name="TitleText" Text="Warning" VerticalAlignment="Center" Margin="10,0" Foreground="#DDDDDD"
        FontSize="16" FontWeight="Bold" />
      <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"
        HorizontalAlignment="Right" Click="Close_Click" Style="{StaticResource CloseButtonStyle}" />
    </Grid>
    <Grid Grid.Row="1" Margin="20">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Path Grid.Column="0" Data="M12,2L1,21H23M12,6L19.53,19H4.47M11,10V14H13V10M11,16V18H13V16"
        Fill="#FFCC00" Width="48" Height="48" Stretch="Uniform" VerticalAlignment="Top"
        Margin="0,10,10,0">
        <Path.Effect>
          <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="#FFCC00" Opacity="0.3" />
        </Path.Effect>
      </Path>
      <TextBlock Grid.Column="1" x:Name="MessageText" Text="Warning message goes here."
        TextWrapping="Wrap" VerticalAlignment="Top" Foreground="#DDDDDD" FontSize="14"
        LineHeight="20" />
    </Grid>
    <Grid Grid.Row="2" Background="#20232a" Height="60">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="CancelButton" Content="Nope! Tactical Retreat!" Click="Close_Click"
          Style="{StaticResource ActionButtonStyle}" Background="Transparent" Margin="0,0,10,0"
          Height="40" MinWidth="50" />
        <Button x:Name="ConfirmButton" Content="I accept my fate!" Click="Confirm_Click"
          Style="{StaticResource PrimaryActionButtonStyle}" Margin="0,0,10,0" Height="40"
          MinWidth="50" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>